// Session state module — reserved for per-connection state management.
// Currently, pgwire handles session metadata internally via ClientInfo.
// This module will hold custom session state as the protocol evolves.
